BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[screens] ADD [readOnly] BIT NOT NULL CONSTRAINT [screens_readOnly_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
