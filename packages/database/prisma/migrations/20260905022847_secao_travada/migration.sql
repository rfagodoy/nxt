BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[screen_sections] ADD [locked] BIT NOT NULL CONSTRAINT [screen_sections_locked_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
