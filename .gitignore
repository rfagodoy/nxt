# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
.next
out

# Turbo
.turbo

# Anexos (storage local de arquivos)
uploads
apps/api/uploads

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode
.idea

# Prisma
packages/database/generated

# Rascunhos e dados de trabalho (scripts de diagnóstico, dumps, logs de build).
# ⚠️ Fica FORA do versionamento porque acaba guardando DADO REAL: os backups de
# limpeza de base levam a tabela de usuários inteira — hash de senha incluso — e a
# senha SMTP cifrada. Um `git add -A` distraído publicaria isso.
scratchpad
